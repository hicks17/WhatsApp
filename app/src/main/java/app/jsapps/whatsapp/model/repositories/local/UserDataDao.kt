package app.jsapps.whatsapp.model.repositories.local

import androidx.room.*

//@Dao
interface UserDataDao {

    /*@Query("SELECT * FROM local_stats3 WHERE id = :typeRec")
    suspend fun getLocalData(typeRec:Int): LocalStatsEntity2?

    @Query("SELECT * FROM conversations WHERE id = :typeData")
    suspend fun getAllConversations(typeData:Int):Int

    @Query("SELECT highestScoreEasyChall FROM local_stats3 WHERE id = :typeData")
    suspend fun getConversation(typeData:Int):Int

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertStats(data: LocalStatsEntity2)

    @Query("DELETE FROM local_stats3 WHERE id = 1")
    suspend fun removeData()*/

}
